# Используем официальный образ Node.js, версия 16
FROM node:16

# Устанавливаем директорию приложения в контейнере
WORKDIR /app

# Копируем package.json и package-lock.json (если есть)
COPY package*.json ./

# Устанавливаем зависимости
RUN npm ci

# Копируем все файлы в директорию приложения
COPY . .

# Копируем файл .env в директорию приложения
#COPY .env .

# Открываем порт, на котором работает приложение
EXPOSE 14035

# Запускаем приложение
CMD [ "npm", "start" ]
