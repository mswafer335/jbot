export class mediumApi {
    token: string;
    url: string;
    constructor(url: string, token: string) {
        this.url = url;
        this.token = token;
    }
    // private request(method: string, url: string, data: any, headers: any) {
    //     axios;
    // }
}
